<% if @page.new_record? %>
  $("input#page_<%= @page.id %>_name").effect('highlight', {color:'red'});
<% else %>
  $('table#nodes').append("<%= escape_javascript(render @node) %>");
  $('tr#<%= dom_id(@node) %>').effect('highlight');
<% end %>